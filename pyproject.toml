[project]
name = "llm-personal-kb"
version = "0.2.0"
description = "Personal knowledge base compiled from AI conversations - uses Kiro CLI headless mode"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "tzdata>=2024.1",
]

[tool.ruff]
line-length = 100
